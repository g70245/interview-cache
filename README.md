# Quick Start
```
# Build and run the tests
docker build -t interview/cache .

# Start a container 
docker run -p 8080:8080 interview/cache

# Visit the test report page to see the results
http://localhost:8080/test/index.html
```